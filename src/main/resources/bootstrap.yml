spring:
  profiles:
    active:
      default
  cloud:
    config:
      #uri: http://localhost:8888
      enabled: true
      name: client-panel
  application:
    name: client-panel
  mvc:
    view:
      prefix:

eureka:
  instance:
    prefer-ip-address: true
    health-check-url-path: /application/health
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url: http://103.227.68.166:8761/eureka
      #discovery:
       # enabled: true
        #service-id: auth-service